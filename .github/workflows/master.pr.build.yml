name: Build on PR

on:
  pull_request:
    branches:
      - master

jobs:
  run:
    name: Build on PR
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2
      - uses: actions/setup-node@v1
        with:
          node-version: 16
      - run: npm install
      - run: npm run update
      - run: npm run build --if-present
      # - run: npm test